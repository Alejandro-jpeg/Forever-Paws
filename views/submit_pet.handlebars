<div class=" mb-4 project" id="blogbox">
    <div>
        <img src="assets/submit_pet_form.png" alt="Submit Pet">
    </div>
    <section class="section ">
        <div class="container card formbgc p-6">
            
            <p>Thank you for choosing to submit a pet for adoption on our portal. Please provide the following information about the pet. All fields marked with an asterisk (*) are required.</p>
            
            <div class="columns">

                <!-- FIRST COLUMN -->
                <div class="column is-half">

                    <!-- Pet name -->
                    <div class="field ">
                        <label class="label ">Pet name</label>
                        <div class="control">
                            <input class="input" type="text" placeholder="Write pet name">
                        </div>
                    </div>
                    <!-- Breed dropdown -->
                    <div class="field">
                        <label class="label">Breed</label>
                        <div class="control">
                            <div class="select">
                                <select>
                                    <!-- Populate options dynamically using homeRoutes -->
                                    <option>Select a Breed</option>
                                    <!-- Add options fetched from the backend here -->
                                </select>
                            </div>
                        </div>
                    </div>
                    <!-- Gender dropdown -->
                    <div class="field">
                        <label class="label">Gender</label>
                        <div class="control">
                            <div class="select">
                                <select>
                                    <!-- Populate options dynamically using homeRoutes-->
                                    <option>Select Gender</option>
                                    <!-- Add options fetched from the backend here -->
                                </select>
                            </div>
                        </div>
                    </div>
                    <!-- Agency dropdown -->
                    <div class="field">
                        <label class="label">Agency</label>
                        <div class="control">
                            <div class="select">
                                <select>
                                    <!-- Populate options dynamically using homeRoutes-->
                                    <option>Select an Agency</option>
                                    <!-- Add options fetched from the backend here -->
                                </select>
                            </div>
                        </div>
                    </div>
                    <br>
                    <!-- Photo -->
                    <div class="file">
                    <label class="file-label">
                        <input class="file-input" type="file" name="resume">
                        <span class="file-cta">
                        <span class="file-icon">
                            <i class="fas fa-upload"></i>
                        </span>
                        <span class="file-label">
                            Choose a pet photo...
                        </span>
                        </span>
                    </label>
                    </div>

                </div>



                <!-- SECOND COLUMN -->
                <div class="column is-half">

                    <!-- Pet type dropdown -->
                    <div class="field">
                        <label class="label">Pet type</label>
                        <div class="control">
                            <div class="select">
                                <select>
                                    <!-- Populate options dynamically using homeRoutes -->
                                    <option>Select a Pet type</option>
                                    <!-- Add options fetched from the backend here -->
                                </select>
                            </div>
                        </div>
                    </div>
                    <!-- Age range dropdown -->
                    <div class="field">
                        <label class="label">Age range</label>
                        <div class="control">
                            <div class="select">
                                <select>
                                    <!-- Populate options dynamically using homeRoutes-->
                                    <option>Select an Age range</option>
                                    <!-- Add options fetched from the backend here -->
                                </select>
                            </div>
                        </div>
                    </div>
                    <!-- Description -->
                    <div class="field">
                        <label class="label">Description</label>
                        <div class="control">
                            <div class="control">
                                <textarea class="textarea" placeholder="Description"></textarea>
                            </div>
                        </div>
                    </div>
                    <!-- City dropdown -->
                    <div class="field">
                        <label class="label">City</label>
                        <div class="control">
                            <div class="select">
                                <select>
                                    <!-- Populate options dynamically using homeRoutes-->
                                    <option>Select a City</option>
                                    <!-- Add options fetched from the backend here -->
                                </select>
                            </div>
                        </div>
                    </div>

                </div>




            </div>

            <!-- Submit button we need to add an event listener and run a query search -->
            <div class="field is-grouped is-justify-content-flex-end">
                <div class="control">
                    <button class="button custom-box-sl">
                        <span class="search-text">Submit</span>
                    </button>
                </div>
            </div>

        </div>
    </section>
</div>
<script src="./js/submitpet.js"></script>